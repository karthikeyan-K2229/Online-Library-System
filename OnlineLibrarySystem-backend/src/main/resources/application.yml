server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/librarydb
    username: root
    password: root
  jpa:
    show-sql: true
    database: mysql
    hibernate:
      ddl-auto: update
